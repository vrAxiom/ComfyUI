# AI Provider Selection
# Options: ollama | lmstudio | openai | gemini
AI_PROVIDER=ollama
AI_MODEL=mistral

# Local Providers
# Ollama default URL
OLLAMA_URL=http://localhost:11434
# LM Studio (OpenAI-compatible server) default URL
LMSTUDIO_BASE_URL=http://localhost:1234/v1

# Cloud Providers (optional; only used if AI_PROVIDER=openai or gemini)
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
GEMINI_API_KEY=
# For Gemini, set region-specific base via client if needed

# Excel Storage (OneDrive path recommended)
# Example: C:\\Users\\<username>\\OneDrive\\Documents\\nViteXtracter\\applications.xlsx
EXCEL_PATH=

# Auto-Trigger Identification Rules
# Comma-separated list of sender emails or domains to match
MATCH_FROM=@naukri.com,@linkedin.com
# Comma-separated keywords to search in subject (case-insensitive contains)
MATCH_SUBJECT_KEYWORDS=application,applied,candidate,profile

# Watcher Settings (Windows only)
WATCH_FOLDER=Inbox
# Polling fallback interval in seconds (if events not firing reliably)
WATCH_POLL_INTERVAL=5
